import { Disposable } from 'using-statement';

export class FileResource implements Disposable {

  constructor(name: string) {
    // Open the file
  }

  dispose(): void {
    // Close the file resource
  }

}
